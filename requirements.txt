# Core dependencies
pypdf>=4.0.0

# Rendering pages to images: pypdfium2 wraps PDFium (Apache-2.0/BSD-3-Clause,
# prebuilt wheels, no Python dependencies of its own), Pillow encodes the PNGs.
pypdfium2>=4.30.0
Pillow>=10.0.0

# MCP support
mcp>=2.0.0

# Optional: For using with Ollama
ollama>=0.1.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
